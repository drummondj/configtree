on:
  push:
    branches:
    - main

jobs:
  test:
    name: Run all tests
    runs-on: ubuntu-22.04
    steps:
    - name: checkout repo
      uses: actions/checkout@main
    - name: Run tests
      run: make test
    
  
